{
  "name": "flash-calendar-custom",
  "private": true,
  "workspaces": [
    "apps/example",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean && rm -rf node_modules",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "packageManager": "npm@10.0.0",
  "devDependencies": {
    "prettier": "^2.8.8",
    "turbo": "^2.0.6",
    "typescript": "~5.9.2"
  }
}
